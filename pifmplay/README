Compile it like
	gcc -lm pifm.c -o pifm

Don't run startx, invoke this from the console. Make
sure nothing else is running on the Raspberry (this 
will take almost all of the CPU).

please make sure to use all are in correct folders